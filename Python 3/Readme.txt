The Python command line API has been ported from Python 2 to Python 3.4+, and now requires numpy.

It is compatible with firmware v14, and will be compatible with all future firmware updates.

The Python API is in development, and should not be considered stable yet.

-JS 
20 July 2017