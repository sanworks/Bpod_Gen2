% sm = Set_StateMatrix(sm, Matrix state_matrix)
%                An alias for SetStateMatrix().  See the help for
%                that function instead.
function [sm] = Set_StateMatrix(varargin)

     sm = SetStateMatrix(varargin);
